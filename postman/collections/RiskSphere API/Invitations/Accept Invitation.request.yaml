$kind: http-request
url: "{{baseUrl}}/organization-invitations/accept"
method: POST
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "token": "{{inviteToken}}"
    }
order: 3000
