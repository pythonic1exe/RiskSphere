import { DashboardCard } from "./dashboard-card"
import type { DashboardData } from "./dashboard-data"

export function AnalyticsCard({ data }: { data: DashboardData }) {
  const points = data.trend.map((value, index) => `${(index / (data.trend.length - 1)) * 100},${100 - value}`).join(" ")
  return <DashboardCard title="GRC readiness" description="Readiness is moving steadily upward across the last nine checkpoints."><div className="flex items-end justify-between gap-5"><div><p className="font-heading text-4xl tracking-[-0.08em] text-text-primary">82%</p><p className="mt-2 text-sm text-success">12% above May baseline</p></div><div className="flex gap-4 text-xs text-text-muted"><span className="flex items-center gap-2"><span className="size-2 rounded-full bg-primary" />Readiness</span><span className="hidden items-center gap-2 sm:flex"><span className="size-2 rounded-full bg-brand-accent" />Target</span></div></div><div className="relative mt-8 h-56 overflow-hidden rounded-[13px] bg-bg-app/70 p-4"><div className="absolute inset-x-4 top-1/4 border-t border-border-subtle/60" /><div className="absolute inset-x-4 top-1/2 border-t border-border-subtle/60" /><div className="absolute inset-x-4 top-3/4 border-t border-border-subtle/60" /><svg viewBox="0 0 100 100" preserveAspectRatio="none" className="relative h-full w-full" role="img" aria-label="GRC readiness trend increasing from 54 percent to 82 percent"><defs><linearGradient id="readiness-fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="var(--brand-primary)" stopOpacity="0.28" /><stop offset="100%" stopColor="var(--brand-primary)" stopOpacity="0" /></linearGradient></defs><polygon points={`0,100 ${points} 100,100`} fill="url(#readiness-fill)" /><polyline points={points} fill="none" stroke="var(--brand-primary)" strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.8" vectorEffect="non-scaling-stroke" /></svg><div className="absolute inset-x-4 bottom-3 flex justify-between text-[10px] text-text-disabled"><span>May</span><span>Jun</span><span>Jul</span><span>Aug</span></div></div></DashboardCard>
}
